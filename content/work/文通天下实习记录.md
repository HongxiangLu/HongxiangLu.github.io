---
title: "文通天下实习记录"
date: 2026-02-26T09:00:00+08:00
slug: intern
draft: false
categories:
    - "工作"
---

## 26-02-26

今天完成的内容：完成入职，领取了一台工作笔记本。在笔记本上安装好了办公软件，以后离职需要清除个人信息的话，只需处理这个安装路径以及Edge浏览器上的信息。

今后的任务：一是调研小智的TTS方案：介于流式与非流式之间的响应逻辑，大约以句子为单位。黄总说已有声网API的成熟方案。二是关于音色如何定制的问题。

## 26-02-27

今天在本地环境跑通了机器。

1. 根据[Deployment.md](https://github.com/xinnan-tech/xiaozhi-esp32-server/blob/main/docs/Deployment.md)在本地通过源代码部署Server。预期结果是可以通过浏览器运行测试网页。 
   * 需要根据[智能硬件PRD](https://rq4r2defnx7.feishu.cn/docx/TsA2dg7c7oLfMixjJDZcN2bZnDg)中的讨论阶段内容修改代码；
   * 在 `.config.yaml` 配置文件中配置 `server.websocket`、`selected_module.LLM` 和 `LLM.ChatGLMLLM.api_key`。

2. 通过配网模式将机器和本地环境连接，参考[firmware-setting.md](https://github.com/xinnan-tech/xiaozhi-esp32-server/blob/main/docs/firmware-setting.md)。
   * 需要配置计算机防火墙，为OTA接口的端口添加入站规则；同时最好将局域网IP设为静态IP。
   * 机器在断开网络连接后，自动进入配网模式并打开自身的AP热点。通过手机连接AP热点，可以配置OTA地址。这个过程中可能需要重启机器。

3. 根据[mqtt-gateway-integration.md](https://github.com/xinnan-tech/xiaozhi-esp32-server/blob/main/docs/mqtt-gateway-integration.md)的第一部分部署MQTT网关。

## 26-03-02

领导说，我这周需要调研如何修改机器的音色和人设。修改人设只要换提示词就可以了，所以主要是去调研各种TTS（Text to Speech）模型。

1. EdgeTTS： 
   * 参考内容：（1）[GitHub仓库](https://github.com/rany2/edge-tts)（2）[知乎教程1](https://zhuanlan.zhihu.com/p/1954331160944545851)（3）[知乎教程2](https://zhuanlan.zhihu.com/p/703686916)
   * 优势：无需下载模型权重、无需注册账号或API，通过pip包使用。小智系统默认使用。

2. MinimaxTTSHTTPStream
   * 具备语音复刻、合成功能；
   * 具有 **RPM（Requests Per Minute）** 和 **TPM（Tokens Per Minute）** 限制；
   * 尚未调试成功。可能原因如下：
     * 官方配置文件 `config.yaml` 标识「当前支持的type为edge、doubao」，其他模型可能没有调通；
     * 参数配置问题，如 `group_id`（可能需要加引号）、`model`（特定模型的API是否需要购买）；
     * 后台日志显示成功调用了模型：「core.providers.tts.minimax_httpstream]-INFO-语音生成成功」。那么问题是否可能出现在硬件上（比如生成语音）？

## 26-03-03

根据领导指示，深入研究 MiniMax 的TTS模型。

1. 在启发下深入研究代码、使用日志调试，成功调通了MinimaxTTSHTTPStream。

2. 成功调通了 MiniMax 的音色复刻功能。

3. 代码是否现在上传到公司仓库？上传多少？还需要和领导确认。

4. 公司的 API Key 有问题，目前还在使用个人账号测试功能。后续需要确认。

## 26-03-04

今天继续研究 MiniMax 的音色复刻功能，复刻了汪曾祺的形象。做的工作包括寻找采访音频、剪辑，以及修改大模型提示词等。

但是还有个缺点：唤醒语音没有更换，机器人唤醒时还是台湾女孩的机车声音。领导注意到这一点，让我换好之后再向大领导展示。他的话很有道理：「做事就把工作做全，不要让人家到时候挑出你的错。」

另外，向领导确认了知识库（RAG）的构建路线：放弃Dify、Coze等低代码开发平台，选择自行构建（可以使用LangChain等框架）。

## 26-03-09

上午：原本设置了手动分配IP的笔记本无法联网，前辈说是IP已经被分配了。于是重新手动设置IP，熟悉了流程：首先设为自动分配，使用ipconfig命令查看地址、掩码和网关，然后手动配置。

下午：了解了开发板类型。领导给的这块是较旧的ESP32（ESP32-WROOM-32E），没有麦克风和喇叭模块，无法支持机器功能。

另外，也了解了代码编译和烧录过程。需要安装ESP-IDF，并使用这种软件来选择开发板类型、配置唤醒词。另外，领导要求的更换开机语音需要在项目中替换。

## 26-03-13

前两天在忙学校的事，好不容易安心工作了一天。 前段时间没有合适的开发板，领导让我了解了下知识库的构建。原来小智这个项目可以通过插件形式增加功能，我尝试让AI写了个知识库。

今天，我拿着领导给的开发板把小智项目（xiaozhi-esp32，属于硬件方面）跑通了。不得不说，AI真是太好用啦！

除此之外，我还解决了音色复刻的事情。原来MiniMax的平台分为国内版和海外版，而领导提供的公司API是海外版的，所以之前连接不上。我还用这个功能帮助了公司的同事。

另外，对于服务器项目（xiaozhi-esp32-server）我还需要好好测试一下，如何将代码添加到公司的Git仓库。